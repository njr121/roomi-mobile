# 기획 문서 및 로그 (루트 docs만 제외)
/docs/

# 환경변수
.env
.env.local
.env*.local

# 패키지
node_modules/

# 빌드 결과물
.next/
dist/

# 시스템 파일
.DS_Store

# Claude Code 개인 설정
CLAUDE.local.md
**/.claude/settings.local.json

# roomi-api는 별도 worktree(roomi-api-server/)에서 작업 — 이 폴더는 로컬 설정만 있는 placeholder
/roomi-api/

# Next.js/TS 빌드 산물 (혹시 다른 위치에 생겨도 차단)
next-env.d.ts
tsconfig.tsbuildinfo
